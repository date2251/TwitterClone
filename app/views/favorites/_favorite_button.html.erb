<% if current_user.favoriting?(tweet) %>
	<%= form_with(model: current_user.favorites.find_by(tweet_id: tweet.id), local: true, method: :delete) do |f| %>
		<%= hidden_field_tag :tweet_id, tweet.id %>
		<%= f.submit 'UNFAV', class: 'btn btn-primary btn-sm' %>
	<% end %>
<% else %>
	<%= form_with(model: current_user.favorites.build, local: true) do |f| %>
		<%= hidden_field_tag :tweet_id, tweet.id %>
		<%= f.submit 'FAV', class: 'btn btn-primary btn-sm' %>
	<% end %>
<% end %>
